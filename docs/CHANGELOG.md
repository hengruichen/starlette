# Changelog

## 0.24.1 (2024-07-29)

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https:***REDACTED***@app.middleware("http")
async def auth_middleware(request: Request, call_next):
    # Do something with the request
    response = await call_next(request)
    # Do something with the response
    return response
```

### Other

- Fix `Request` and `Response` to work with `AnyIO` 4.0.0. ([#1165](https://github.com/encode/starlette/issues/1165))
- Fix `Request` and `Response